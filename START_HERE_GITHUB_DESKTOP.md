# Publish a new repository under HHFinAi

This package is prepared locally, not already published. It contains no `.git` history. Keep the complete folder, including hidden `.github`, `.gitignore` and `.gitattributes` files. Do not upload the ZIP as the project source.

In GitHub Desktop, sign in to the intended account and confirm the repository owner is **HHFinAi**. Create a new local repository named `transition-plan-credibility-agent` in an empty location. Copy this package's contents into that local folder while preserving the newly created `.git` directory. Review every changed file. Run the tests before committing. Check Git author name/email and use a privacy-preserving email when appropriate. Choose **Publish repository**, select visibility deliberately, and verify the owner/name before publication. Do not overwrite or force-push an existing repository.

For an existing repository, first fetch and pull it, create a working branch, copy reviewed changes into it without replacing `.git`, and use a pull request. Never delete old work or change visibility automatically.

Use `repository-metadata.json` for the About description and topics. The intended repository URL is not evidence of publication. Only after publishing should links, homepage/canonical URL and any status badges be updated to real URLs. GitHub Actions is configured but has not run remotely in this delivery. Optional `docs/index.html` is a static landing-page template; hosting is not configured automatically.
